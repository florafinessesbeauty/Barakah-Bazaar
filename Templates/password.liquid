{% layout 'theme' %}
<section class="page-width text-center">
  <h1>Barakah Bazaar — Coming Soon</h1>
  <p>We’re preparing something special. Sign up to be notified when we launch.</p>
  {% form 'customer' %}
    <input type="email" name="contact[email]" placeholder="Enter your email" required>
    <button type="submit">Notify Me</button>
  {% endform %}
</section>
